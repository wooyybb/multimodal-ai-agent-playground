# Prompts

Prompt archives are stored in `docs/prompts/`.

The current documentation refactor keeps only the latest prompt archive in that folder to reduce duplication:

- `docs/prompts/039_context_program_builder.md`

Historical sprint narratives remain in `docs/sprint_book/`.

## Future Work

- Add curated prompt templates instead of storing every raw sprint prompt.
- Keep only prompts that explain reusable engineering patterns.
