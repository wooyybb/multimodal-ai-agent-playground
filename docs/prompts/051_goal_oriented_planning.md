# Sprint 51 Prompt Archive

## Task

Add Goal-oriented Planning before execution planning.

## Architecture Prompt

The target flow is `User Prompt -> Goal Planner -> Goal Tree -> Planner -> Execution Plan`.

## Files Allowed

`agents/goal_planner.py`, planner, execution engine, tool registry, debug report, README, and docs.

## Done Definition

Goal Tree is generated, planner and execution engine are connected, prompt compilation receives priority context, debug report stores `goal_tree`, and the framework remains stable.
