class ReflectionAgent:
    def run(self, score, prompt):
        print("[ReflectionAgent] Running...")
        return (
            "Review the prompt for clarity, stronger visual details, "
            "and better alignment with the target score."
        )
